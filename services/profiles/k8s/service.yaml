apiVersion: v1
kind: Service
metadata:
  name: profiles-service
  labels:
    app: profiles-service
spec:
  selector:
    app: profiles-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
